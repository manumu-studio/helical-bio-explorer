// Next.js configuration.
// Deployment target: Vercel. No special output mode — Vercel's build infra handles artifact layout.

import type { NextConfig } from "next";

const nextConfig: NextConfig = {};

export default nextConfig;
